import 'package:flutter/material.dart';

class KidsInformationScreen extends StatelessWidget {
  const KidsInformationScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}
